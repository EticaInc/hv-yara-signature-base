<?php
/**
 * Positive fixture for PHP_Supervueity_Obfuscator_CUST.
 *
 * Exercises the "2 of" branch: the rule requires <?php at offset 0 plus at least two of its
 * four generated-identifier markers. Two are present below.
 *
 * Inert by construction. The markers this obfuscator family emits are meaningless generated
 * identifiers, so a fixture that carries them needs no functional code at all -- there is no
 * payload here, no network call, and nothing to execute.
 */

$charles_multiapiment = 1;
$microapplicationic_megaalgorithmive = 2;
